using UnityEngine;
using UnityEngine.UI;
using TMPro;
using Remalux.WallPainting;

namespace Remalux.WallPainting.UI
{
      [RequireComponent(typeof(WallPainter))]
      public class WallPainterUI : MonoBehaviour
      {
            // ... existing code ...
      }
}